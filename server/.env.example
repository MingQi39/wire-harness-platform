# 复制为 .env 后按需修改

DB_HOST=127.0.0.1
DB_PORT=5434
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=wire_harness_dev
DB_SSL_MODE=disable

REDIS_HOST=127.0.0.1
REDIS_PORT=6381

APP_ENV=development
APP_PORT=8081

# 与 web/.env.development 中 VITE_PASSWORD_ENCRYPT_KEY 须一致
PASSWORD_ENCRYPT_KEY=da6b71d7f087bceec4b116994472c499

# 首次部署种子管理员 bjy 仍为默认密码时，migrate/启动会同步为此值
# LIMS_ADMIN_PASSWORD=qwer.123

CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

FRONTEND_DEV_PORT=5173
